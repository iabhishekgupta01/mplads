import { useEffect, useMemo, useState } from 'react'
import { Activity, AlertTriangle, ArrowUpRight, BarChart3, Bell, Check, ChevronDown, CircleHelp, ClipboardCheck, Clock3, FileText, Filter, IndianRupee, LayoutDashboard, Map, Menu, Search, ShieldCheck, SlidersHorizontal, TrendingUp, Users, X, Zap } from 'lucide-react'
import { Area, AreaChart, Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import StrictLandingPage from './pages/LandingPage.jsx'
import { Metric, PageHeader, Panel, Progress, ProjectTable, RiskBadge, StatCard, StatusBadge } from './components/common/Primitives.jsx'
import { formatCurrency, riskLabel } from './utils/formatters.js'
import { pathFromView, viewFromPath } from './routes/pathRoutes.js'
import './App.css'

const projectsSeed = [
  { id: 'MP-2024-1001', name: 'Road Construction - Sehore', state: 'Madhya Pradesh', district: 'Sehore', category: 'Road', amount: 2000000, expenditure: 85, physical: 40, score: 82, status: 'Delayed', finding: 'Financial-progress mismatch', delay: '4 months', vendorId: 'VEND-019', vendor: 'ABC Infrastructure', agency: 'MP Rural Works' },
  { id: 'MP-2024-1187', name: 'Construction of Community Hall', state: 'Madhya Pradesh', district: 'Indore', category: 'Community Infrastructure', amount: 4850000, expenditure: 92, physical: 31, score: 92, status: 'Under Review', finding: 'Suspicious billing pattern', delay: '6 months', vendorId: 'VEND-019', vendor: 'ABC Infrastructure', agency: 'MP Urban Development' },
  { id: 'MP-2025-0421', name: 'Primary Health Centre Upgrade', state: 'Madhya Pradesh', district: 'Bhopal', category: 'Health', amount: 3200000, expenditure: 78, physical: 60, score: 68, status: 'Ongoing', finding: 'Elevated cost risk', delay: '2 months', vendorId: 'VEND-044', vendor: 'CivicBuild Works', agency: 'MP Health Engineering' },
  { id: 'RJ-2024-0742', name: 'Drinking Water Pipeline', state: 'Rajasthan', district: 'Kota', category: 'Water', amount: 2800000, expenditure: 61, physical: 57, score: 43, status: 'Ongoing', finding: 'No immediate concern', delay: 'On track', vendorId: 'VEND-031', vendor: 'JalSetu Projects', agency: 'Rajasthan PHED' },
  { id: 'BR-2025-0120', name: 'Government School Infrastructure Upgrade', state: 'Bihar', district: 'Gaya', category: 'Education', amount: 4100000, expenditure: 94, physical: 48, score: 89, status: 'Delayed', finding: 'Vendor payment anomaly', delay: '5 months', vendorId: 'VEND-056', vendor: 'Eastern Buildtech', agency: 'Bihar Education Works' },
  { id: 'MH-2024-0312', name: 'Community Sanitation Facility', state: 'Maharashtra', district: 'Nashik', category: 'Sanitation', amount: 1650000, expenditure: 45, physical: 48, score: 22, status: 'Ongoing', finding: 'No immediate concern', delay: 'On track', vendorId: 'VEND-061', vendor: 'Nirmal Infra', agency: 'Maharashtra Water Board' },
]
const vendorsSeed = [
  { id: 'VEND-019', name: 'ABC Infrastructure', risk: 86, projects: 3, payments: 12, value: 4250000, flagged: 4, states: 'Madhya Pradesh', districts: 'Sehore, Indore', finding: 'Repeated similar payment amounts' },
  { id: 'VEND-044', name: 'CivicBuild Works', risk: 62, projects: 2, payments: 7, value: 2380000, flagged: 1, states: 'Madhya Pradesh', districts: 'Bhopal, Vidisha', finding: 'Payment frequency requires review' },
  { id: 'VEND-031', name: 'JalSetu Projects', risk: 38, projects: 4, payments: 15, value: 5120000, flagged: 0, states: 'Rajasthan', districts: 'Kota, Ajmer', finding: 'No immediate concern' },
  { id: 'VEND-056', name: 'Eastern Buildtech', risk: 78, projects: 2, payments: 9, value: 3460000, flagged: 3, states: 'Bihar', districts: 'Gaya, Patna', finding: 'Transactions clustered in short periods' },
  { id: 'VEND-061', name: 'Nirmal Infra', risk: 29, projects: 3, payments: 8, value: 1970000, flagged: 0, states: 'Maharashtra', districts: 'Nashik, Pune', finding: 'No immediate concern' },
]
const paymentsSeed = [
  { id: 'TXN-001', projectId: 'MP-2024-1001', vendorId: 'VEND-019', amount: 500000, date: '2026-08-22', type: 'Milestone payment', status: 'Flagged', flagged: true, reason: 'Three identical payments detected within 48 hours' },
  { id: 'TXN-002', projectId: 'MP-2024-1001', vendorId: 'VEND-019', amount: 500000, date: '2026-08-23', type: 'Milestone payment', status: 'Under Review', flagged: true, reason: 'Repeated amount pattern' },
  { id: 'TXN-003', projectId: 'MP-2024-1001', vendorId: 'VEND-019', amount: 500000, date: '2026-08-24', type: 'Milestone payment', status: 'Processed', flagged: true, reason: 'Payment clustered within 48 hours' },
  { id: 'TXN-004', projectId: 'MP-2024-1187', vendorId: 'VEND-019', amount: 875000, date: '2026-08-28', type: 'Advance', status: 'Processed', flagged: false, reason: 'No immediate concern' },
  { id: 'TXN-005', projectId: 'BR-2025-0120', vendorId: 'VEND-056', amount: 725000, date: '2026-08-12', type: 'Milestone payment', status: 'Flagged', flagged: true, reason: 'Unusual payment frequency' },
  { id: 'TXN-006', projectId: 'RJ-2024-0742', vendorId: 'VEND-031', amount: 420000, date: '2026-08-08', type: 'Milestone payment', status: 'Released', flagged: false, reason: 'No immediate concern' },
]
const inspectionsSeed = [{ id: 'INSP-1001', projectId: 'MP-2024-1001', officer: 'Anita Sharma', priority: 'High', date: '2026-09-12', status: 'Pending', reason: 'Verify reported physical progress and payment records.', reported: 40, observed: null }, { id: 'INSP-1002', projectId: 'MP-2024-1187', officer: 'Rakesh Verma', priority: 'High', date: '2026-09-14', status: 'Scheduled', reason: 'Review delayed community infrastructure work.', reported: 31, observed: null }, { id: 'INSP-1003', projectId: 'BR-2025-0120', officer: 'Meera Kumari', priority: 'Medium', date: '2026-09-15', status: 'Submitted', reason: 'Validate vendor payment anomaly.', reported: 48, observed: 45 }]
const progressSeed = [{ id: 'PROG-001', projectId: 'MP-2024-1001', percent: 35, date: '2026-09-05', by: 'District Authority', role: 'District Authority', remarks: 'Submitted progress report.', verification: 'Verified' }, { id: 'PROG-002', projectId: 'MP-2024-1001', percent: 40, date: '2026-09-10', by: 'Field Officer', role: 'Field Officer', remarks: 'Road base work reported complete.', verification: 'Pending Verification' }]
const trendData = ['Oct','Nov','Dec','Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep'].map((month, index) => ({ month, high: 310 + index * 11, medium: 980 + index * 28, low: 3580 + index * 77 }))
const fundData = [{ state: 'Uttar Pradesh', allocated: 82, utilized: 61 }, { state: 'Maharashtra', allocated: 68, utilized: 55 }, { state: 'Bihar', allocated: 59, utilized: 39 }, { state: 'Madhya Pradesh', allocated: 54, utilized: 42 }, { state: 'Rajasthan', allocated: 46, utilized: 36 }]
const auditSeed = [{ time: '05 Sep 2026, 04:18 PM', actor: 'AI Risk Engine', action: 'Risk score updated', detail: '68 → 82 · New transaction anomaly detected.', tone: 'ai' }, { time: '01 Sep 2026, 11:02 AM', actor: 'System', action: 'Project flagged', detail: 'Financial-progress mismatch detected.', tone: 'system' }, { time: '15 Aug 2026, 09:30 AM', actor: 'District Authority', action: 'Progress report submitted', detail: 'Physical progress reported at 40%.', tone: 'human' }]
const navItems = [{ label: 'Dashboard', icon: LayoutDashboard, view: 'dashboard' }, { label: 'Project Monitoring', icon: ClipboardCheck, view: 'projects' }, { label: 'Risk & Alerts', icon: AlertTriangle, view: 'alerts', badge: '12' }, { label: 'Analytics', icon: BarChart3, view: 'analytics' }, { label: 'Field Operations', icon: ClipboardCheck, view: 'field-dashboard' }, { label: 'Inspections', icon: ShieldCheck, view: 'inspections' }, { label: 'Payments', icon: IndianRupee, view: 'payments' }, { label: 'Vendors', icon: Users, view: 'vendors' }, { label: 'GIS Map', icon: Map, view: 'map' }, { label: 'Fund Utilization', icon: IndianRupee, view: 'funds' }, { label: 'Reports', icon: FileText, view: 'reports' }, { label: 'Compliance', icon: ShieldCheck, view: 'compliance' }, { label: 'User Management', icon: Users, view: 'users' }, { label: 'Settings', icon: SlidersHorizontal, view: 'settings' }]

function App() {
  const initialPath = window.location.pathname
  const [view, setView] = useState(viewFromPath(initialPath))
  const [entry, setEntry] = useState(initialPath === '/' ? 'landing' : 'login')
  const [loggedIn, setLoggedIn] = useState(initialPath !== '/' && initialPath !== '/login')
  const [projects, setProjects] = useState(projectsSeed)
  const [vendors] = useState(vendorsSeed)
  const [payments, setPayments] = useState(paymentsSeed)
  const [inspections, setInspections] = useState(inspectionsSeed)
  const [progress, setProgress] = useState(progressSeed)
  const [selectedId, setSelectedId] = useState(initialPath.startsWith('/projects/') ? initialPath.split('/').pop() : null)
  const [activeVendorId, setActiveVendorId] = useState(initialPath.startsWith('/vendors/') ? initialPath.split('/').pop() : null)
  const [activePaymentId, setActivePaymentId] = useState(initialPath.startsWith('/payments/') ? initialPath.split('/').pop() : null)
  const [activeInspectionId, setActiveInspectionId] = useState(initialPath.startsWith('/inspections/') ? initialPath.split('/').pop() : null)
  const [query, setQuery] = useState('')
  const [riskFilter, setRiskFilter] = useState('All')
  const [modal, setModal] = useState(null)
  const [audit, setAudit] = useState(auditSeed)
  const [toast, setToast] = useState('')
  const [mobileNav, setMobileNav] = useState(false)
  const selected = projects.find((project) => project.id === selectedId) || projects[0]
  const filteredProjects = useMemo(() => projects.filter((project) => `${project.id} ${project.name} ${project.district} ${project.state}`.toLowerCase().includes(query.toLowerCase()) && (riskFilter === 'All' || riskLabel(project.score) === riskFilter.toUpperCase())), [projects, query, riskFilter])
  const showToast = (message) => { setToast(message); window.setTimeout(() => setToast(''), 3200) }
  useEffect(() => { const onPopState = () => { const path = window.location.pathname; setEntry(path === '/' ? 'landing' : 'login'); setLoggedIn(path !== '/' && path !== '/login'); setView(viewFromPath(path)); const id = path.startsWith('/projects/') ? path.split('/').pop() : null; setSelectedId(id); }; window.addEventListener('popstate', onPopState); return () => window.removeEventListener('popstate', onPopState) }, [])
  const openProject = (id) => { setSelectedId(id); setView('detail'); setEntry('login'); setLoggedIn(true); window.history.pushState({}, '', `/projects/${id}`); setMobileNav(false) }
  const navigate = (next) => { setView(next); setEntry('login'); setLoggedIn(true); window.history.pushState({}, '', pathFromView(next)); setMobileNav(false); if (next !== 'detail') setSelectedId(null) }
  const submitAction = (event) => { event.preventDefault(); const action = modal; const status = action === 'audit' ? 'Under Review' : action === 'halt' ? 'Funds Halted' : 'Resolved'; const actionLabel = action === 'audit' ? 'Requested Field Audit' : action === 'halt' ? 'Halted vendor payment' : 'Marked project resolved'; setProjects((current) => current.map((project) => project.id === selected.id ? { ...project, status } : project)); setAudit((current) => [{ time: '06 Sep 2026, 10:42 AM', actor: 'State Nodal Officer', action: actionLabel, detail: `${selected.name} · Officer action recorded.`, tone: 'human' }, ...current]); setModal(null); showToast(action === 'audit' ? 'Audit request submitted. Project is now under review.' : action === 'halt' ? 'Vendor payment workflow placed on hold.' : 'Project marked as resolved.') }
  void LandingPage

  if (entry === 'landing') return <StrictLandingPage onEnter={() => { setEntry('login'); window.history.pushState({}, '', '/login') }} />
  if (!loggedIn) return <LoginPage onLogin={() => { setLoggedIn(true); setView('dashboard'); window.history.pushState({}, '', '/dashboard') }} />

  return (
    <div className="app-shell"><Sidebar view={view} navigate={navigate} mobileNav={mobileNav} setMobileNav={setMobileNav} /><div className="app-main"><header className="topbar"><div className="topbar-left"><button className="icon-button mobile-menu" onClick={() => setMobileNav(true)} aria-label="Open navigation"><Menu size={20} /></button><div className="crumb">MPLADS / <strong>{view === 'detail' ? 'Project Investigation' : view[0].toUpperCase() + view.slice(1)}</strong></div></div><div className="global-search"><Search size={17} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search projects, districts, MPs..." /></div><div className="topbar-actions"><span className="live"><i /> Live data <small>Updated 2 min ago</small></span><button className="icon-button notification" aria-label="Notifications"><Bell size={18} /><b>3</b></button><div className="user-chip"><span>SN</span><div><strong>State Nodal Officer</strong><small>Madhya Pradesh</small></div><ChevronDown size={15} /></div></div></header><main className="page-content">{view === 'dashboard' && <Dashboard projects={projects} openProject={openProject} navigate={navigate} />}{view === 'projects' && <Projects projects={filteredProjects} query={query} setQuery={setQuery} riskFilter={riskFilter} setRiskFilter={setRiskFilter} openProject={openProject} />}{view === 'detail' && <ProjectDetail project={selected} openModal={setModal} navigate={navigate} />}{view === 'progress' && <ProgressPage project={selected} progress={progress} setProgress={setProgress} setProjects={setProjects} showToast={showToast} navigate={navigate} />}{view === 'verification' && <VerificationPage project={selected} inspections={inspections} setInspections={setInspections} setProjects={setProjects} setAudit={setAudit} showToast={showToast} navigate={navigate} />}{view === 'field-dashboard' && <FieldDashboard inspections={inspections} projects={projects} openProject={openProject} navigate={navigate} />}{view === 'inspections' && <Inspections inspections={inspections} projects={projects} openInspection={(id) => { setActiveInspectionId(id); window.history.pushState({}, '', `/inspections/${id}`); setView('inspection-detail') }} />}{view === 'inspection-detail' && <InspectionDetail inspection={inspections.find((item) => item.id === activeInspectionId) || inspections[0]} project={projects.find((item) => item.id === (inspections.find((inspection) => inspection.id === activeInspectionId) || inspections[0]).projectId) || selected} inspections={inspections} setInspections={setInspections} setProjects={setProjects} setProgress={setProgress} setAudit={setAudit} showToast={showToast} navigate={navigate} />}{view === 'vendors' && <Vendors vendors={vendors} openVendor={(id) => { setActiveVendorId(id); window.history.pushState({}, '', `/vendors/${id}`); setView('vendor-detail') }} />}{view === 'vendor-detail' && <VendorDetail vendor={vendors.find((item) => item.id === activeVendorId) || vendors[0]} projects={projects} payments={payments} openProject={openProject} navigate={navigate} />}{view === 'payments' && <Payments payments={payments} projects={projects} vendors={vendors} openPayment={(id) => { setActivePaymentId(id); window.history.pushState({}, '', `/payments/${id}`); setView('payment-detail') }} />}{view === 'payment-detail' && <PaymentDetail payment={payments.find((item) => item.id === activePaymentId) || payments[0]} project={projects.find((item) => item.id === (payments.find((payment) => payment.id === activePaymentId) || payments[0]).projectId) || selected} vendor={vendors.find((item) => item.id === (payments.find((payment) => payment.id === activePaymentId) || payments[0]).vendorId) || vendors[0]} setPayments={setPayments} setAudit={setAudit} showToast={showToast} navigate={navigate} />}{view === 'compliance' && <Compliance audit={audit} />}{view === 'alerts' && <Alerts projects={projects} openProject={openProject} />}{view === 'analytics' && <AnalyticsCenter projects={projects} vendors={vendors} payments={payments} openProject={openProject} navigate={navigate} />}{view === 'map' && <GisPage projects={projects} openProject={openProject} navigate={navigate} />}{['funds', 'reports', 'users', 'settings'].includes(view) && <SecondaryPage view={view} projects={projects} navigate={navigate} />}</main></div>{modal && <ActionModal type={modal} project={selected} onClose={() => setModal(null)} onSubmit={submitAction} />}{toast && <div className="toast"><Check size={17} /> {toast}</div>}</div>
  )
}

function LandingPage({ onEnter }) {
  return <div className="public-page">
    <a className="skip-link" href="#landing-content">Skip to content</a>
    <header className="public-header"><div className="public-utility"><span>Government of India</span><span>Prototype / Demonstration Environment</span><span>Accessibility</span></div><div className="public-nav"><button className="public-brand" onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}><span className="brand-mark">M</span><span><strong>MPLADS AI</strong><small>Monitoring System</small></span></button><nav><a href="#overview">Overview</a><a href="#capabilities">Capabilities</a><a href="#how-it-works">How it works</a><a href="#faq">FAQ</a></nav><button className="primary" onClick={onEnter}>Access portal <ArrowUpRight size={16} /></button></div></header>
    <main id="landing-content">
      <section className="landing-hero" id="overview"><div className="hero-copy"><p className="eyebrow">MPLADS · SIH 26102</p><h1>Monitor what matters.<br /><em>Investigate what stands out.</em></h1><p className="hero-lede">AI-assisted monitoring for project execution, financial utilization, anomaly detection and accountable officer action.</p><div className="hero-actions"><button className="primary" onClick={onEnter}>Access monitoring portal <ArrowUpRight size={17} /></button><a className="secondary" href="#how-it-works">Explore how it works <ArrowUpRight size={16} /></a></div><p className="hero-note"><ShieldCheck size={15} /> Decision support for human-led verification, not automated findings of fraud.</p></div><LandingPreview onEnter={onEnter} /></section>
      <div className="trust-strip"><span><Activity size={17} /> AI-assisted monitoring</span><span><AlertTriangle size={17} /> Project risk intelligence</span><span><IndianRupee size={17} /> Financial visibility</span><span><ShieldCheck size={17} /> Action traceability</span></div>
      <section className="landing-section challenge-section"><div className="section-intro"><p className="eyebrow">THE MONITORING CHALLENGE</p><h2>Signals are most useful when they arrive together.</h2></div><div className="challenge-copy"><p>Project execution produces financial, physical, geographic and transaction information. The challenge is turning those signals into a clear next step for the officer responsible for the work.</p><div className="challenge-stat"><strong>45<span>pt</span></strong><span>gap between expenditure and reported physical progress in the showcase project.</span></div></div></section>
      <section className="landing-section flow-section" id="how-it-works"><div className="section-intro"><p className="eyebrow">FROM DATA TO ACTION</p><h2>A connected monitoring workflow.</h2><p>Move from a state-level signal to project evidence without losing context.</p></div><div className="workflow"><FlowStep number="01" title="Monitor" text="Projects · funds · progress" icon={LayoutDashboard} /><FlowStep number="02" title="Detect" text="AI risk signals" icon={AlertTriangle} /><FlowStep number="03" title="Investigate" text="Evidence · explanation · prediction" icon={Search} /><FlowStep number="04" title="Act" text="Audit · review · resolution" icon={ClipboardCheck} /><FlowStep number="05" title="Audit" text="Traceable officer action" icon={ShieldCheck} /></div></section>
      <section className="landing-section capability-section" id="capabilities"><div className="section-intro"><p className="eyebrow">CAPABILITIES</p><h2>Intelligence that stays explainable.</h2><p>Each signal is designed to help an officer decide what to verify next.</p></div><div className="capability-grid"><Capability icon={TrendingUp} title="Cost and time prediction" text="See whether current expenditure and progress point toward an overrun or delay." /><Capability icon={Activity} title="Transaction anomaly detection" text="Surface repeated, unusual or closely timed payment patterns for verification." /><Capability icon={Map} title="Geographic risk intelligence" text="Locate concentration by state and district, then drill into priority works." /><Capability icon={ShieldCheck} title="Explainable risk scoring" text="Connect a score to reasons, evidence, predictions and a recommended action." /></div></section>
      <section className="landing-section risk-preview-section"><div className="section-intro"><p className="eyebrow">EXPLAINABLE AI</p><h2>A score is only the beginning.</h2><p>The officer should see the evidence behind a signal and the action it suggests.</p></div><div className="risk-preview"><div className="risk-preview-head"><div><span className="eyebrow">PROJECT INVESTIGATION · MP-2024-1001</span><h3>Road Construction – Sehore</h3><p>Sehore, Madhya Pradesh · Road · Delayed</p></div><RiskBadge score={82} /></div><div className="risk-preview-body"><div className="preview-score"><strong>82</strong><span>/ 100</span><b>HIGH RISK</b></div><div className="preview-reasons"><h4>Why is this project flagged?</h4><p><AlertTriangle size={15} /> 85% expenditure vs 40% physical progress</p><p><Clock3 size={15} /> 4-month delay indicated</p><p><AlertTriangle size={15} /> 3 similar payments within 48 hours</p><p><Search size={15} /> Possible nearby similar project · 65%</p></div><div className="preview-action"><span>Recommended action</span><strong>Field verification</strong><p>Check reported physical progress and payment records before further disbursement.</p><button className="text-button" onClick={onEnter}>Open investigation preview <ArrowUpRight size={15} /></button></div></div></div></section>
      <section className="landing-section roles-section"><div className="section-intro"><p className="eyebrow">DESIGNED FOR PUBLIC-SECTOR TEAMS</p><h2>One view, progressive levels of responsibility.</h2></div><div className="role-list"><Role title="Ministry Officials" text="National and cross-state visibility" /><Role title="State Nodal Officers" text="State and district monitoring" /><Role title="District Authorities" text="Local investigation and action" /><Role title="Field Officers" text="Assigned inspections and evidence" /></div></section>
      <section className="landing-section faq-section" id="faq"><div className="section-intro"><p className="eyebrow">CLARITY BY DESIGN</p><h2>Questions officers and evaluators ask.</h2></div><div className="faq-list"><details open><summary>Does the AI confirm fraud?</summary><p>No. It produces risk signals and anomaly indications to prioritize projects for human verification and investigation.</p></details><details><summary>What evidence is shown to officers?</summary><p>Financial and physical progress, payment patterns, project history, similarity signals and model predictions are connected to each alert.</p></details><details><summary>Can the platform integrate with existing systems?</summary><p>Yes. The prototype separates pages from service modules so the mock data layer can be replaced with the planned Express and AI services.</p></details></div></section>
    </main><footer className="public-footer"><div><strong>MPLADS AI Monitoring System</strong><p>Prototype for SIH Problem Statement 26102.</p></div><div><span>Monitor</span><span>Detect</span><span>Investigate</span><span>Act</span></div><button className="primary" onClick={onEnter}>Access portal <ArrowUpRight size={16} /></button></footer>
  </div>
}

function LandingPreview({ onEnter }) { return <div className="landing-preview"><div className="preview-toolbar"><span><i /> Live monitoring scope</span><small>FY 2025–26</small></div><div className="preview-kpis"><div><span>High-risk projects</span><strong>427</strong><small>+18.6% this month</small></div><div><span>Fund utilization</span><strong>73.3%</strong><small>Across current scope</small></div></div><div className="preview-map"><div className="map-shape"><span className="map-dot dot-1" /><span className="map-dot dot-2" /><span className="map-dot dot-3" /><span className="map-label">Madhya Pradesh · 72</span></div><div><span className="eyebrow">RISK CONCENTRATION</span><strong>58 high-risk works</strong><small>View state intelligence →</small></div></div><button className="preview-alert" onClick={onEnter}><span><AlertTriangle size={16} /> Priority alert</span><strong>Road Construction – Sehore</strong><small>85% expenditure · 40% physical progress <b>82 HIGH</b></small></button></div> }
function FlowStep({ number, title, text, icon: Icon }) { return <div className="flow-step"><span>{number}</span><div className="flow-icon"><Icon size={18} /></div><h3>{title}</h3><p>{text}</p></div> }
function Capability({ icon: Icon, title, text }) { return <article className="capability"><div className="capability-icon"><Icon size={19} /></div><h3>{title}</h3><p>{text}</p></article> }
function Role({ title, text }) { return <div className="role"><strong>{title}</strong><span>{text}</span><ArrowUpRight size={16} /></div> }

function LoginPage({ onLogin }) { return <div className="login-page"><div className="login-brand"><div className="brand-mark large">M</div><p className="eyebrow">MINISTRY OF STATISTICS & PROGRAMME IMPLEMENTATION</p><h1>MPLADS<br /><span>AI Monitoring System</span></h1><p className="login-copy">A decision-support platform for transparent project execution, explainable risk intelligence and accountable action.</p><div className="login-signal"><span><Activity size={17} /> Monitor</span><b>→</b><span><AlertTriangle size={17} /> Detect</span><b>→</b><span><ShieldCheck size={17} /> Act</span></div></div><div className="login-panel"><p className="eyebrow">SECURE OFFICER ACCESS</p><h2>Welcome back</h2><p className="muted">Sign in to your monitoring workspace.</p><label>User ID / Email<input placeholder="officer@mplads.gov.in" defaultValue="officer@mplads.gov.in" /></label><label>Password<input type="password" placeholder="Enter password" defaultValue="prototype" /></label><label>Role<select defaultValue="State Nodal Officer"><option>State Nodal Officer</option><option>Ministry Official</option><option>District Authority</option></select></label><button className="primary full" onClick={onLogin}>Sign in to monitoring system <ArrowUpRight size={17} /></button><p className="login-note"><CircleHelp size={14} /> Prototype environment · Synthetic monitoring data</p></div></div> }
function Sidebar({ view, navigate, mobileNav, setMobileNav }) { return <aside className={`sidebar ${mobileNav ? 'open' : ''}`}><div className="sidebar-brand"><div className="brand-mark">M</div><div><strong>MPLADS AI</strong><span>Monitoring System</span></div><button className="icon-button close-nav" onClick={() => setMobileNav(false)} aria-label="Close navigation"><X size={19} /></button></div><div className="sidebar-rule" /><div className="scope"><span>ACTIVE SCOPE</span><strong>Madhya Pradesh</strong><small>FY 2025–26</small></div><nav>{navItems.map(({ label, icon: Icon, view: itemView, badge }) => <button key={itemView} className={`nav-item ${view === itemView ? 'active' : ''}`} onClick={() => navigate(itemView)}><Icon size={18} /><span>{label}</span>{badge && <b>{badge}</b>}</button>)}</nav><div className="sidebar-footer"><div className="footer-line"><ShieldCheck size={16} /><span>Prototype environment</span></div><p>Transparent development,<br />stronger India.</p><small>v0.1 · SIH 26102</small></div></aside> }
function Dashboard({ projects, openProject, navigate }) { return <><PageHeader eyebrow="STATE MONITORING · MADHYA PRADESH" title="MPLADS Monitoring Overview" subtitle="Track project execution, fund utilization and AI-detected risks." action={<button className="secondary"><FileText size={16} /> Export report</button>} /><div className="stat-grid"><StatCard label="Allocated funds" value="₹5,892 Cr" trend="6.2%" icon={IndianRupee} /><StatCard label="Released funds" value="₹4,320 Cr" trend="5.1%" icon={ArrowUpRight} /><StatCard label="Fund utilization" value="73.3%" trend="4.8%" icon={BarChart3} /><StatCard label="Total projects" value="48,326" trend="12.4%" icon={ClipboardCheck} /><StatCard label="High-risk projects" value="427" trend="18.6%" icon={AlertTriangle} critical /></div><div className="chart-grid"><Panel title="Risk trend" subtitle="Project risk distribution over the last 12 months"><ResponsiveContainer width="100%" height={245}><AreaChart data={trendData} margin={{ top: 8, right: 4, left: -20, bottom: 0 }}><CartesianGrid vertical={false} stroke="#e7eceb" /><XAxis dataKey="month" axisLine={false} tickLine={false} tick={{ fill: '#85918e', fontSize: 11 }} /><YAxis axisLine={false} tickLine={false} tick={{ fill: '#85918e', fontSize: 11 }} /><Tooltip contentStyle={{ border: '1px solid #dfe7e4', borderRadius: 10, fontSize: 12 }} /><Area type="monotone" dataKey="low" stroke="#46a982" strokeWidth={2} fill="none" /><Area type="monotone" dataKey="medium" stroke="#d79a31" strokeWidth={2} fill="none" /><Area type="monotone" dataKey="high" stroke="#c94b45" strokeWidth={2.5} fill="none" /></AreaChart></ResponsiveContainer><div className="legend"><span><i className="low" /> Low risk</span><span><i className="medium" /> Medium risk</span><span><i className="high" /> High risk</span></div></Panel><Panel title="Fund allocation vs utilization" subtitle="State-wise comparison in ₹ crore"><ResponsiveContainer width="100%" height={245}><BarChart data={fundData} margin={{ top: 8, right: 0, left: -20, bottom: 0 }}><CartesianGrid vertical={false} stroke="#e7eceb" /><XAxis dataKey="state" axisLine={false} tickLine={false} tick={{ fill: '#85918e', fontSize: 10 }} tickFormatter={(value) => value === 'Madhya Pradesh' ? 'MP' : value.split(' ')[0]} /><YAxis axisLine={false} tickLine={false} tick={{ fill: '#85918e', fontSize: 11 }} /><Tooltip contentStyle={{ border: '1px solid #dfe7e4', borderRadius: 10, fontSize: 12 }} /><Bar dataKey="allocated" fill="#203f52" radius={[4, 4, 0, 0]} barSize={13} /><Bar dataKey="utilized" fill="#58a783" radius={[4, 4, 0, 0]} barSize={13} /></BarChart></ResponsiveContainer><div className="legend"><span><i className="allocated" /> Allocated</span><span><i className="utilized" /> Utilized</span></div></Panel></div><Panel title="Geographic risk overview" subtitle="Risk concentration across states" action={<button className="text-button" onClick={() => navigate('map')}>Open investigation map <ArrowUpRight size={15} /></button>}><div className="geo-overview"><div className="india-graphic"><div className="map-shape"><span className="map-dot dot-1" /><span className="map-dot dot-2" /><span className="map-dot dot-3" /><span className="map-dot dot-4" /><span className="map-dot dot-5" /><span className="map-dot dot-6" /><span className="map-label">Madhya Pradesh</span></div></div><div className="geo-list">{[['Madhya Pradesh', 'High', 72, 58], ['Bihar', 'High', 69, 44], ['Rajasthan', 'Medium', 51, 31], ['Maharashtra', 'Low', 38, 19]].map(([name, risk, score, count]) => <button key={name} className="geo-row" onClick={() => navigate('projects')}><span className={`risk-dot ${risk.toLowerCase()}`} /><strong>{name}</strong><span className={`risk-text ${risk.toLowerCase()}`}>{risk}</span><span>{count} flagged</span><b>{score}</b><ArrowUpRight size={15} /></button>)}</div></div></Panel><div className="bottom-grid"><Panel title="AI flagged projects" subtitle="Highest-risk projects requiring attention" action={<button className="text-button" onClick={() => navigate('projects')}>View all <ArrowUpRight size={15} /></button>}><ProjectTable projects={projects.filter((project) => project.score >= 68).slice(0, 4)} openProject={openProject} /></Panel><Panel title="AI insights" subtitle="Signals requiring officer attention"><div className="insight-list"><Insight icon={TrendingUp} title="High-risk projects increased by 35%" text="Concentration is strongest across 7 districts in the current period." /><Insight icon={Activity} title="18 projects show progress gaps" text="Expenditure is materially ahead of reported physical progress." /><Insight icon={Zap} title="Recurring vendor patterns detected" text="Payment behavior requires verification across multiple works." /></div></Panel></div></> }
function Insight({ icon: Icon, title, text }) { return <div className="insight"><div className="insight-icon"><Icon size={17} /></div><div><strong>{title}</strong><p>{text}</p></div><ArrowUpRight size={15} /></div> }
function Projects({ projects, query, setQuery, riskFilter, setRiskFilter, openProject }) { return <><PageHeader eyebrow="PROJECT DATABASE" title="Project Monitoring" subtitle="Search and analyze MPLADS works across states, districts and constituencies." /><div className="filter-bar"><div className="filter-search"><Search size={17} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search project, district or ID..." /></div><div className="filter-select"><Filter size={15} /><select value={riskFilter} onChange={(event) => setRiskFilter(event.target.value)}><option>All</option><option>High</option><option>Medium</option><option>Low</option></select></div><button className="secondary"><SlidersHorizontal size={16} /> More filters</button><span className="result-count">{projects.length} projects found</span></div><Panel title="Project register" subtitle="AI-scored works in the current monitoring scope"><ProjectTable projects={projects} openProject={openProject} /></Panel></> }
function ProjectDetail({ project, openModal, navigate }) { const gap = project.expenditure - project.physical; return <><button className="back-link" onClick={() => navigate('projects')}>← Back to project monitoring</button><div className="detail-head"><div><p className="eyebrow">PROJECT INVESTIGATION · {project.id}</p><h1>{project.name}</h1><p className="muted">{project.category} · {project.district}, {project.state} · Sample execution agency</p></div><div className="detail-status"><RiskBadge score={project.score} /><StatusBadge status={project.status} /></div></div><div className="metric-strip"><Metric label="Sanctioned amount" value={formatCurrency(project.amount)} /><Metric label="Released amount" value="₹18.0 L" /><Metric label="Expenditure" value="₹17.0 L" note={`${project.expenditure}% utilized`} /><Metric label="Physical progress" value={`${project.physical}%`} note="Reported progress" /><Metric label="Expected completion" value="Jun 2026" /><Metric label="Estimated delay" value={project.delay} critical /></div><div className="detail-grid"><Panel title="Financial vs physical progress" subtitle="A material gap can indicate a need for field verification."><div className="progress-block"><Progress label="Financial progress" value={project.expenditure} tone="navy" /><Progress label="Physical progress" value={project.physical} tone="amber" /><div className="gap-callout"><div><span>Progress gap</span><strong>{gap} percentage points</strong></div><p>Expenditure is significantly higher than reported physical progress.</p></div></div></Panel><Panel title="AI risk score" subtitle="Explainable risk intelligence"><div className="risk-analysis"><div className="score-ring" style={{ '--score': `${project.score * 3.6}deg` }}><div><strong>{project.score}</strong><span>/ 100</span><b>{riskLabel(project.score)} RISK</b></div></div><div className="score-note"><ShieldCheck size={18} /><strong>Score combines financial, time and transaction signals.</strong><p>AI output is a monitoring indicator, not a finding of wrongdoing.</p></div></div></Panel></div><div className="investigation-grid"><Panel title="Why is this project flagged?" subtitle="Plain-language explanation of the current score"><div className="reason-list"><Reason title="Financial-progress mismatch" text={`${project.expenditure}% expenditure against ${project.physical}% physical progress.`} /><Reason title="Project delay detected" text={`Current trajectory indicates an estimated ${project.delay} delay.`} /><Reason title="Payment anomaly" text="Three similar payments were detected within a short period." /><Reason title="Similar project pattern" text="A nearby project shows unusually similar attributes." /></div></Panel><Panel title="AI predictions" subtitle="Current model outputs"><div className="prediction-grid"><Prediction label="Delay probability" value="78%" /><Prediction label="Cost overrun risk" value="65%" /><Prediction label="Anomaly score" value={`${project.score}`} /></div><div className="prediction-foot"><span>Predicted final cost</span><strong>₹23.6 L <small>+18%</small></strong><span>Predicted completion</span><strong>Aug 2026 <small>+4 months</small></strong></div></Panel></div><Panel title="Payment / transaction analysis" subtitle="Evidence connected to the AI flag"><div className="table-wrap"><table><thead><tr><th>Date</th><th>Transaction ID</th><th>Vendor</th><th>Amount</th><th>Description</th><th>AI status</th></tr></thead><tbody>{['14 Aug 2026', '14 Aug 2026', '15 Aug 2026'].map((date, index) => <tr key={date + index}><td>{date}</td><td>TXN-00{index + 1}</td><td>ABC Infrastructure</td><td>₹5,00,000</td><td>Road material payment</td><td><span className="flagged"><AlertTriangle size={14} /> Flagged</span></td></tr>)}</tbody></table></div><div className="transaction-callout"><AlertTriangle size={18} /><div><strong>Pattern detected</strong><p>3 identical payments released within 48 hours. Requires verification.</p></div></div></Panel><div className="action-band"><div><p className="eyebrow">RECOMMENDED ACTION</p><h2>Field verification recommended</h2><p>Check reported physical progress and payment records before further disbursement.</p></div><div className="action-buttons"><button className="primary" onClick={() => openModal('audit')}><ClipboardCheck size={16} /> Request audit</button><button className="danger" onClick={() => openModal('halt')}><AlertTriangle size={16} /> Halt payment</button><button className="secondary" onClick={() => openModal('resolve')}><Check size={16} /> Mark resolved</button></div></div></> }
function Reason({ title, text }) { return <div className="reason"><span><AlertTriangle size={16} /></span><div><strong>{title}</strong><p>{text}</p></div></div> }
function Prediction({ label, value }) { return <div className="prediction"><span>{label}</span><strong>{value}</strong><small>Model indicator</small></div> }
function Alerts({ projects, openProject }) { return <><PageHeader eyebrow="AI ANALYSIS" title="Risk & Alerts" subtitle="Prioritize projects requiring investigation." /><div className="alert-summary"><div><span>Unresolved alerts</span><strong>12</strong></div><div><span>High risk</span><strong className="red">7</strong></div><div><span>Under review</span><strong>4</strong></div><div><span>Resolved this month</span><strong className="green">18</strong></div></div><div className="alert-grid">{projects.filter((project) => project.score >= 68).map((project) => <article className="alert-card" key={project.id}><div className="alert-card-top"><RiskBadge score={project.score} /><span>Detected 2 hours ago</span></div><h2>{project.name}</h2><p>{project.district}, {project.state}</p><div className="triggered"><strong>Triggered by</strong><span>• {project.expenditure}% expenditure · {project.physical}% physical progress</span><span>• {project.finding}</span></div><button className="primary small" onClick={() => openProject(project.id)}>Investigate <ArrowUpRight size={15} /></button></article>)}</div></> }
function Compliance({ audit }) { return <><PageHeader eyebrow="ACCOUNTABILITY" title="Compliance & Audit Trail" subtitle="A transparent record of system signals and officer decisions." action={<button className="secondary"><FileText size={16} /> Export trail</button>} /><div className="compliance-banner"><ShieldCheck size={22} /><div><strong>Audit integrity active</strong><p>All risk changes and officer actions are recorded against the project history.</p></div><span>● Live trail</span></div><Panel title="Recent activity" subtitle="MPLADS monitoring events · Madhya Pradesh"><div className="timeline">{audit.map((event, index) => <div className="timeline-item" key={`${event.time}-${index}`}><div className={`timeline-dot ${event.tone}`}><span /></div><div className="timeline-content"><div className="timeline-meta"><span>{event.time}</span><b>{event.actor}</b></div><h3>{event.action}</h3><p>{event.detail}</p></div></div>)}</div></Panel></> }
function SecondaryPage({ view, projects, navigate }) { const content = { funds: ['Fund Utilization', 'Understand where program money is being used across the current state scope.', '₹5,892 Cr', '73.3% utilized'], map: ['GIS Risk Map', 'Investigate geographic concentration and navigate into high-risk works.', '58 high-risk works', 'Madhya Pradesh'], analytics: ['Risk Analytics', 'Compare risk, financial execution, delay and anomaly signals by jurisdiction.', '427 high-risk projects', '7 districts need enhanced monitoring'], reports: ['Monitoring Reports', 'Prepare an executive view of project health, risk findings and officer actions.', 'FY 2025–26', 'Report center'], users: ['User Management', 'Manage monitoring roles and administrative scope.', '24 active officers', 'Access directory'], settings: ['System Settings', 'Configure profile, notifications, accessibility and monitoring thresholds.', '80–100', 'High-risk threshold'] }[view]; return <><PageHeader eyebrow="MPLADS OPERATIONS" title={content[0]} subtitle={content[1]} action={<button className="primary"><FileText size={16} /> {view === 'reports' ? 'Generate report' : 'Open workspace'}</button>} /><div className="secondary-hero"><div className="secondary-mark"><Map size={24} /></div><div><p className="eyebrow">CURRENT STATE SCOPE</p><h2>{content[2]}</h2><p>{content[3]} · Prototype data connected</p></div></div><div className="placeholder-grid"><Panel title="Priority view" subtitle="The next operational signal for your team"><div className="placeholder-stat"><AlertTriangle size={20} /><div><strong>{projects.filter((project) => project.score >= 80).length + 55}</strong><span>works need enhanced monitoring</span></div><ArrowUpRight size={18} /></div><button className="text-button" onClick={() => navigate('projects')}>Open project register <ArrowUpRight size={15} /></button></Panel><Panel title="System note" subtitle="Backend-ready prototype"><p className="placeholder-copy">This screen is wired to the same structured mock service layer used by the dashboard. Replace service calls with the Express API when the backend is ready.</p><div className="service-row"><Check size={16} /> Mock service connected</div><div className="service-row"><Clock3 size={16} /> Last synchronized 2 min ago</div></Panel></div></> }
function ActionModal({ type, project, onClose, onSubmit }) { const labels = { audit: ['Request field audit', 'Submit audit request'], halt: ['Halt vendor payment?', 'Confirm halt'], resolve: ['Mark project resolved', 'Confirm resolution'] }; return <div className="modal-backdrop" onMouseDown={onClose}><div className="modal" onMouseDown={(event) => event.stopPropagation()}><button className="modal-close icon-button" onClick={onClose} aria-label="Close modal"><X size={18} /></button><p className="eyebrow">OFFICER ACTION</p><h2>{labels[type][0]}</h2><p className="muted">{type === 'halt' ? 'You are about to place the associated vendor payment workflow on hold.' : 'Record this decision against the project compliance trail.'}</p><div className="modal-project"><strong>{project.name}</strong><span>{project.id} · {project.district}</span></div>{type === 'audit' && <><label>Assign officer<select defaultValue="Field Officer"><option>Field Officer</option><option>District Authority</option><option>State Nodal Officer</option></select></label><label>Priority<select defaultValue="High"><option>High</option><option>Medium</option></select></label></>}<label>{type === 'resolve' ? 'Resolution notes' : 'Reason'}<textarea defaultValue={type === 'audit' ? 'Verify reported physical progress and payment records.' : ''} placeholder="Enter a concise reason..." /></label><div className="modal-actions"><button className="secondary" onClick={onClose}>Cancel</button><button className={type === 'halt' ? 'danger' : 'primary'} onClick={onSubmit}>{labels[type][1]} <ArrowUpRight size={16} /></button></div></div></div> }

function FieldDashboard({ inspections, projects, openProject }) { const pending = inspections.filter((item) => item.status !== 'Verified'); return <><PageHeader eyebrow="FIELD OPERATIONS" title="Field Operations" subtitle="Assigned inspections, site evidence and verification work." /><div className="stat-grid"><StatCard label="Assigned projects" value={pending.length} trend="Current" icon={ClipboardCheck} /><StatCard label="Pending inspections" value={pending.filter((item) => item.status === 'Pending').length} trend="Needs action" icon={Clock3} critical /><StatCard label="Inspections due" value="2" trend="This week" icon={AlertTriangle} /><StatCard label="Submitted reports" value={inspections.filter((item) => item.status === 'Submitted').length} trend="Current" icon={FileText} /></div><Panel title="Today's inspections" subtitle="Start a visit to capture observed progress, photos and GPS evidence."><div className="table-wrap"><table><thead><tr><th>Inspection</th><th>Project</th><th>Priority</th><th>Scheduled</th><th>Status</th><th /></tr></thead><tbody>{pending.map((item) => { const project = projects.find((entry) => entry.id === item.projectId); return <tr key={item.id}><td><strong>{item.id}</strong></td><td><button className="project-link" onClick={() => openProject(item.projectId)}>{project?.name}<small>{project?.district}</small></button></td><td><RiskBadge score={item.priority === 'High' ? 82 : 62} /></td><td>{item.date}</td><td><StatusBadge status={item.status} /></td><td><button className="primary small" onClick={() => window.history.pushState({}, '', `/inspections/${item.id}`)}>Start inspection</button></td></tr> })}</tbody></table></div></Panel></> }
function Inspections({ inspections, projects, openInspection }) { return <><PageHeader eyebrow="FIELD OPERATIONS" title="Inspections" subtitle="Track the complete AI flag to verification workflow." /><Panel title="Inspection register" subtitle="Reported and observed progress remain separate until supervisor review."><div className="table-wrap"><table><thead><tr><th>Inspection ID</th><th>Project</th><th>Officer</th><th>Priority</th><th>Date</th><th>Status</th><th /></tr></thead><tbody>{inspections.map((item) => <tr key={item.id}><td><strong>{item.id}</strong></td><td>{projects.find((project) => project.id === item.projectId)?.name}</td><td>{item.officer}</td><td>{item.priority}</td><td>{item.date}</td><td><StatusBadge status={item.status} /></td><td><button className="text-button" onClick={() => openInspection(item.id)}>Open <ArrowUpRight size={14} /></button></td></tr>)}</tbody></table></div></Panel></> }
// eslint-disable-next-line no-unused-vars
function InspectionDetail({ inspection, project, inspections, setInspections, setProjects, setProgress, setAudit, showToast, navigate }) { const [observed, setObserved] = useState(inspection.observed ?? '31'); const [remarks, setRemarks] = useState(''); const start = () => { setInspections((items) => items.map((item) => item.id === inspection.id ? { ...item, status: 'In Progress' } : item)); showToast('Inspection started. Capture site evidence before submission.'); }; const submit = (event) => { event.preventDefault(); setInspections((items) => items.map((item) => item.id === inspection.id ? { ...item, observed: Number(observed), remarks, status: 'Submitted', submittedAt: '06 Sep 2026' } : item)); setProgress((items) => [{ id: `PROG-${items.length + 1}`, projectId: project.id, percent: Number(observed), date: '2026-09-06', by: inspection.officer, role: 'Field Officer', remarks, verification: 'Pending Verification' }, ...items]); setProjects((items) => items.map((item) => item.id === project.id ? { ...item, status: 'Verification Pending' } : item)); setAudit((items) => [{ time: '06 Sep 2026, 11:20 AM', actor: inspection.officer, action: 'Inspection submitted', detail: `${project.name} · Observed progress ${observed}%.`, tone: 'human' }, ...items]); showToast('Inspection submitted for supervisor verification.'); }; return <><button className="back-link" onClick={() => navigate('inspections')}>← Back to inspections</button><PageHeader eyebrow="INSPECTION DETAIL" title={project.name} subtitle={`${inspection.id} · ${project.district}, ${project.state}`} action={<RiskBadge score={project.score} />} /><div className="detail-grid"><Panel title="Inspection workflow" subtitle="AI Flagged → Site Visit → Evidence → Supervisor Review"><div className="workflow-strip"><strong>AI Flagged</strong><span>→</span><strong className="active">Site Visit</strong><span>→</span><strong>Verification Pending</strong></div><div className="metric-strip"><Metric label="Reported progress" value={`${inspection.reported}%`} /><Metric label="Observed progress" value={inspection.observed ? `${inspection.observed}%` : 'Not recorded'} critical={inspection.observed && inspection.observed < inspection.reported} /><Metric label="Difference" value={inspection.observed ? `${inspection.observed - inspection.reported} pts` : 'Pending'} /></div></Panel><Panel title="Site visit evidence" subtitle="Demo / Synthetic Evidence"><form className="form-stack" onSubmit={submit}><label>Observed physical progress (%)<input type="number" min="0" max="100" value={observed} onChange={(event) => setObserved(event.target.value)} required /></label><label>Site condition<select defaultValue="Requires attention"><option>Good</option><option>Requires attention</option><option>Critical</option></select></label><label>GPS location<input defaultValue="23.2035° N, 77.4010° E · GPS Verified" readOnly /></label><label>Officer remarks<textarea value={remarks} onChange={(event) => setRemarks(event.target.value)} placeholder="Describe observed work and site condition..." required /></label><label>Upload site photos<input type="file" accept="image/*" multiple /></label><div className="action-buttons"><button type="button" className="secondary" onClick={start}>Start inspection</button><button className="primary">Submit inspection <ArrowUpRight size={15} /></button></div></form></Panel></div></> }
function Vendors({ vendors, openVendor }) { return <><PageHeader eyebrow="PAYMENTS & VENDORS" title="Vendor Intelligence" subtitle="Review cross-project payment behaviour without labelling conclusions as findings of fraud." /><Panel title="Vendor register" subtitle="Elevated risk indicates a need for verification."><div className="table-wrap"><table><thead><tr><th>Vendor</th><th>Projects</th><th>Total payments</th><th>Flagged</th><th>Risk</th><th /></tr></thead><tbody>{vendors.map((vendor) => <tr key={vendor.id}><td><button className="project-link" onClick={() => openVendor(vendor.id)}><strong>{vendor.name}</strong><small>{vendor.id}</small></button></td><td>{vendor.projects}</td><td>{formatCurrency(vendor.value)}</td><td>{vendor.flagged}</td><td><RiskBadge score={vendor.risk} /></td><td><button className="text-button" onClick={() => openVendor(vendor.id)}>Investigate <ArrowUpRight size={14} /></button></td></tr>)}</tbody></table></div></Panel></> }
function VendorDetail({ vendor, projects, payments, openProject, navigate }) { const vendorProjects = projects.filter((project) => project.vendorId === vendor.id); const vendorPayments = payments.filter((payment) => payment.vendorId === vendor.id); return <><button className="back-link" onClick={() => navigate('vendors')}>← Back to vendors</button><PageHeader eyebrow="VENDOR INVESTIGATION" title={vendor.name} subtitle={`${vendor.id} · ${vendor.states} · ${vendor.districts}`} action={<RiskBadge score={vendor.risk} />} /><div className="stat-grid"><StatCard label="Projects handled" value={vendor.projects} trend="Cross-project" icon={ClipboardCheck} /><StatCard label="Payment value" value={formatCurrency(vendor.value)} trend="Recorded" icon={IndianRupee} /><StatCard label="Flagged transactions" value={vendor.flagged} trend="Requires review" icon={AlertTriangle} critical /><StatCard label="Average transaction" value={formatCurrency(vendor.value / Math.max(vendor.payments, 1))} trend="Calculated" icon={Activity} /></div><div className="detail-grid"><Panel title="AI vendor analysis" subtitle="Prototype / synthetic model output"><div className="reason"><span><AlertTriangle size={16} /></span><div><strong>Elevated Vendor Risk</strong><p>{vendor.finding}. Requires verification across projects and transactions.</p></div></div></Panel><Panel title="Projects handled" subtitle="Click through to project investigation"><ProjectTable projects={vendorProjects} openProject={openProject} /></Panel></div><Panel title="Payment timeline" subtitle="Related transactions"><div className="table-wrap"><table><thead><tr><th>Transaction</th><th>Project</th><th>Amount</th><th>Status</th><th>AI reason</th></tr></thead><tbody>{vendorPayments.map((payment) => <tr key={payment.id}><td>{payment.id}</td><td>{payment.projectId}</td><td>{formatCurrency(payment.amount)}</td><td><StatusBadge status={payment.status} /></td><td>{payment.reason}</td></tr>)}</tbody></table></div></Panel></> }
function Payments({ payments, projects, vendors, openPayment }) { return <><PageHeader eyebrow="PAYMENTS & VENDORS" title="Payment Investigation" subtitle="Inspect transaction patterns, related projects and payment status." /><Panel title="Payment register" subtitle="Flagged patterns are investigation signals, not confirmed findings."><div className="table-wrap"><table><thead><tr><th>Transaction</th><th>Project</th><th>Vendor</th><th>Amount</th><th>Date</th><th>Status</th><th>AI flag</th></tr></thead><tbody>{payments.map((payment) => <tr key={payment.id} onClick={() => openPayment(payment.id)}><td><button className="project-link"><strong>{payment.id}</strong><small>{payment.type}</small></button></td><td>{projects.find((project) => project.id === payment.projectId)?.name}</td><td>{vendors.find((vendor) => vendor.id === payment.vendorId)?.name}</td><td>{formatCurrency(payment.amount)}</td><td>{payment.date}</td><td><StatusBadge status={payment.status} /></td><td>{payment.flagged ? <RiskBadge score={82} /> : 'Normal'}</td></tr>)}</tbody></table></div></Panel></> }
function PaymentDetail({ payment, project, vendor, setPayments, setAudit, showToast, navigate }) { const hold = () => { setPayments((items) => items.map((item) => item.id === payment.id ? { ...item, status: 'On Hold', heldBy: 'State Nodal Officer', heldDate: '06 Sep 2026' } : item)); setAudit((items) => [{ time: '06 Sep 2026, 11:35 AM', actor: 'State Nodal Officer', action: 'Payment held', detail: `${payment.id} · ${formatCurrency(payment.amount)} · ${payment.reason}`, tone: 'human' }, ...items]); showToast('Payment placed on hold successfully.'); }; return <><button className="back-link" onClick={() => navigate('payments')}>← Back to payments</button><PageHeader eyebrow="PAYMENT INVESTIGATION" title={payment.id} subtitle={`${project.name} · ${vendor.name}`} action={<StatusBadge status={payment.status} />} /><div className="detail-grid"><Panel title="Transaction details" subtitle="Payment evidence and related entities"><div className="metric-strip"><Metric label="Amount" value={formatCurrency(payment.amount)} /><Metric label="Payment date" value={payment.date} /><Metric label="Vendor risk" value={`${vendor.risk} / 100`} critical={vendor.risk >= 80} /><Metric label="AI risk" value={payment.flagged ? 'HIGH' : 'LOW'} /></div><div className="reason"><span><AlertTriangle size={16} /></span><div><strong>{payment.flagged ? 'Potentially unusual payment behavior' : 'No immediate concern'}</strong><p>{payment.reason}</p></div></div></Panel><Panel title="Officer action" subtitle="Actions are recorded in the compliance trail"><button className="danger" onClick={hold} disabled={payment.status === 'On Hold'}>Hold payment <ArrowUpRight size={15} /></button><p className="muted">{payment.status === 'On Hold' ? `Held by ${payment.heldBy} on ${payment.heldDate}.` : 'Place this transaction on hold while evidence is reviewed.'}</p></Panel></div></> }
function ProgressPage({ project, progress, setProgress, setProjects, showToast, navigate }) { const [percent, setPercent] = useState(project.physical); const [remarks, setRemarks] = useState(''); const submit = (event) => { event.preventDefault(); setProgress((items) => [{ id: `PROG-${items.length + 1}`, projectId: project.id, percent: Number(percent), date: '2026-09-06', by: 'State Nodal Officer', role: 'State Nodal Officer', remarks, verification: 'Pending Verification' }, ...items]); setProjects((items) => items.map((item) => item.id === project.id ? { ...item, physical: Number(percent), status: 'Verification Pending' } : item)); showToast('Progress update saved and marked for verification.'); }; return <><button className="back-link" onClick={() => navigate('detail')}>← Back to project</button><PageHeader eyebrow="PROGRESS UPDATE" title={project.name} subtitle="Demo / Synthetic Evidence · GPS capture is simulated." /><div className="detail-grid"><Panel title="Update physical progress" subtitle="Every submission creates a progress record and audit event."><form className="form-stack" onSubmit={submit}><label>Physical progress (%)<input type="number" min="0" max="100" value={percent} onChange={(event) => setPercent(event.target.value)} required /></label><label>Work completed<textarea value={remarks} onChange={(event) => setRemarks(event.target.value)} placeholder="Describe completed work..." required /></label><label>Update date<input type="date" defaultValue="2026-09-06" /></label><label>GPS location<input defaultValue="23.2035° N, 77.4010° E · GPS Verified" readOnly /></label><label>Upload site photos<input type="file" accept="image/*" multiple /></label><button className="primary">Submit progress update <ArrowUpRight size={15} /></button></form></Panel><Panel title="Progress history" subtitle="Reported progress and verification status"><div className="timeline">{progress.filter((item) => item.projectId === project.id).map((item) => <div className="timeline-item" key={item.id}><div className="timeline-dot human"><span /></div><div className="timeline-content"><div className="timeline-meta"><span>{item.date}</span><b>{item.by}</b></div><h3>{item.percent}% · {item.verification}</h3><p>{item.remarks}</p></div></div>)}</div></Panel></div></> }
function VerificationPage({ project, inspections, setInspections, setProjects, setAudit, showToast, navigate }) { const inspection = inspections.find((item) => item.projectId === project.id); const submit = (event) => { event.preventDefault(); setInspections((items) => items.map((item) => item.id === inspection?.id ? { ...item, status: 'Under Review', verificationResult: 'Major Discrepancy' } : item)); setProjects((items) => items.map((item) => item.id === project.id ? { ...item, score: 91, status: 'Escalated', finding: 'Observed progress below reported progress' } : item)); setAudit((items) => [{ time: '06 Sep 2026, 11:50 AM', actor: 'State Nodal Officer', action: 'Verification submitted', detail: `${project.name} · Risk reassessed to 91 HIGH.`, tone: 'human' }, ...items]); showToast('Verification submitted. Risk score recalculated to 91 HIGH.'); }; return <><button className="back-link" onClick={() => navigate('detail')}>← Back to project</button><PageHeader eyebrow="SUPERVISOR VERIFICATION" title="Verify field evidence" subtitle={project.name} /><Panel title="Reported vs observed progress" subtitle="Potential discrepancy requires human review."><div className="metric-strip"><Metric label="Reported progress" value={`${project.physical}%`} /><Metric label="Observed progress" value="31%" critical /><Metric label="Difference" value={`${31 - project.physical} points`} critical /><Metric label="Updated risk" value="91 / 100 HIGH" critical /></div><form className="form-stack" onSubmit={submit}><label>Verification result<select defaultValue="Major Discrepancy"><option>Verified</option><option>Minor Discrepancy</option><option>Major Discrepancy</option><option>Escalate</option></select></label><label>Payment evidence checked<select defaultValue="Yes"><option>Yes</option><option>No</option></select></label><label>Officer remarks<textarea defaultValue="Observed progress is below reported progress. Additional financial and field verification recommended." /></label><button className="primary">Submit verification <ArrowUpRight size={15} /></button></form></Panel></> }
function AnalyticsCenter({ projects, vendors, payments, openProject, navigate }) { const [scope, setScope] = useState('State'); const [state, setState] = useState('Madhya Pradesh'); const [risk, setRisk] = useState('All'); const filtered = projects.filter((project) => project.state === state && (risk === 'All' || riskLabel(project.score) === risk.toUpperCase())); const high = filtered.filter((project) => project.score >= 80); return <><PageHeader eyebrow="AI ANALYTICS & INTELLIGENCE CENTER" title="Investigate signals by scope" subtitle="Filters and drill-downs are connected to the shared prototype data." /><div className="filter-bar"><label>Analysis scope<select value={scope} onChange={(event) => setScope(event.target.value)}><option>National</option><option>State</option><option>District</option><option>Project</option><option>Vendor</option><option>Payment / Transaction</option></select></label><label>State<select value={state} onChange={(event) => setState(event.target.value)}><option>Madhya Pradesh</option><option>Rajasthan</option><option>Bihar</option><option>Maharashtra</option></select></label><label>Risk<select value={risk} onChange={(event) => setRisk(event.target.value)}><option>All</option><option>High</option><option>Medium</option><option>Low</option></select></label></div><div className="stat-grid"><StatCard label="Projects in scope" value={filtered.length} trend={scope} icon={ClipboardCheck} /><StatCard label="High-risk projects" value={high.length} trend="Investigate" icon={AlertTriangle} critical /><StatCard label="Average risk" value={`${filtered.length ? Math.round(filtered.reduce((sum, item) => sum + item.score, 0) / filtered.length) : 0}`} trend="Score / 100" icon={Activity} /><StatCard label="Payment anomalies" value={payments.filter((payment) => payment.flagged && filtered.some((project) => project.id === payment.projectId)).length} trend="Linked transactions" icon={IndianRupee} /></div><div className="detail-grid"><Panel title="High-risk projects" subtitle={`Filtered to ${state}`}><ProjectTable projects={high} openProject={openProject} /></Panel><Panel title="Vendor anomalies" subtitle="Click a vendor to continue the investigation"><div className="table-wrap"><table><thead><tr><th>Vendor</th><th>Risk</th><th>Flagged</th><th /></tr></thead><tbody>{vendors.filter((vendor) => vendor.risk >= 60).map((vendor) => <tr key={vendor.id}><td>{vendor.name}</td><td><RiskBadge score={vendor.risk} /></td><td>{vendor.flagged}</td><td><button className="text-button" onClick={() => { window.history.pushState({}, '', `/vendors/${vendor.id}`); navigate('vendors') }}>Open vendor <ArrowUpRight size={14} /></button></td></tr>)}</tbody></table></div></Panel></div><Panel title="AI insights" subtitle="Prototype / mock model outputs"><div className="insight-grid"><Insight icon={AlertTriangle} title="Risk concentration" text={`${state} has ${high.length} high-risk project${high.length === 1 ? '' : 's'} in the current prototype scope.`} /><Insight icon={TrendingUp} title="Financial-progress gap" text="Road and community infrastructure projects should be checked where expenditure leads reported physical progress." /><Insight icon={ShieldCheck} title="Next step" text="Use field verification before treating an anomaly as a confirmed finding." /></div></Panel></> }
function GisPage({ projects, openProject }) { const [risk, setRisk] = useState('All'); const shown = projects.filter((project) => risk === 'All' || riskLabel(project.score) === risk.toUpperCase()); return <><PageHeader eyebrow="GEOGRAPHIC INTELLIGENCE" title="GIS Risk Map" subtitle="Project-level markers use synthetic coordinates and shared risk state." /><div className="filter-bar"><label>Risk layer<select value={risk} onChange={(event) => setRisk(event.target.value)}><option>All</option><option>High</option><option>Medium</option><option>Low</option></select></label><span className="result-count">{shown.length} project markers</span></div><div className="map-board"><div className="map-grid-art"><span className="map-india-label">INDIA · PROJECT DISTRIBUTION</span>{shown.map((project, index) => <button key={project.id} className={`map-marker ${riskLabel(project.score).toLowerCase()}`} style={{ left: `${20 + (index * 13) % 70}%`, top: `${22 + (index * 19) % 58}%` }} onClick={() => openProject(project.id)} title={`${project.name} · ${project.score} ${riskLabel(project.score)}`}><i />{project.score}</button>)}</div><div className="map-legend"><strong>Project markers</strong><span><i className="high" /> High risk</span><span><i className="medium" /> Medium risk</span><span><i className="low" /> Low risk</span><p>Click a marker to open the project investigation.</p></div></div></> }

export { Alerts, AnalyticsCenter, Compliance, Dashboard, FieldDashboard, GisPage, InspectionDetail, Inspections, LoginPage, PaymentDetail, Payments, ProjectDetail, Projects, ProgressPage, SecondaryPage, VendorDetail, Vendors, VerificationPage }
export default App
